"use client";
const fs = require("fs");
const path = require("path");
const directoryPath = path.join(__dirname, "./"); // Replace 'your-folder' with your target folder  // Function to prepend  ; to a file const prependToFile = (filePath) => {   const data = fs.readFileSync(filePath, 'utf8');   if (!data.startsWith(' ;')) { // Avoid duplicating     const newData = ` ;\n${data}`;     fs.writeFileSync(filePath, newData, 'utf8');     console.log(`Prepended  ; to ${filePath}`);   } };  // Function to recursively process all JavaScript files in a directory const processDirectory = (dirPath) => {   fs.readdirSync(dirPath).forEach((file) => {     const filePath = path.join(dirPath, file);     if (fs.lstatSync(filePath).isDirectory()) {       processDirectory(filePath); // Recursively process subdirectories     } else if (path.extname(filePath) === '.js') {       prependToFile(filePath);     }   }); };  // Start processing from the target directory processDirectory(directoryPath);
